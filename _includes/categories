<div class="ai-categories">
{% for cat in site.categories %}
    <a class="btn btn-{% cycle 'primary', 'danger', 'success', 'info', 'warning', 'inverse', 'default' %}" href="{{site.BASE_PATH}}/category.html#{{ cat[0] }}-ref">
        {{ cat[0] }}
        <span class="badge">{{ cat[1].size }}</span>
    </a>
{% endfor %}
</div>